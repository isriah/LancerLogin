{
  "name": "@lancerlogin/api",
  "version": "0.6.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "build": "esbuild ./src/index.ts --bundle --format=esm --platform=browser --outfile=dist/index.js",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "wrangler": "^4.30.0"
  },
  "dependencies": {
    "@noble/hashes": "^2.0.0"
  }
}
