polkit.addRule(function(action, subject) {
  if (subject.user === "lancerlogin" && action.id === "org.freedesktop.systemd1.manage-units" && action.lookup("unit") === "lancerlogin-update.service") {
    return polkit.Result.YES;
  }
});
