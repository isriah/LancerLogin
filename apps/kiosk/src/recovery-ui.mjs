export const recoveryApp = `let reloadToken;async function checkForReload(){try{const response=await fetch("/display-state",{cache:"no-store"});if(!response.ok)return;const state=await response.json();if(reloadToken!==undefined&&state.displayReloadToken!==reloadToken){location.reload();return}reloadToken=state.displayReloadToken}catch{}}await checkForReload();setInterval(checkForReload,2000);`;
