{
  "name": "@lancerlogin/telemetry-collector",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild ./src/index.ts --bundle --format=esm --platform=browser --outfile=dist/index.js",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "wrangler": "^4.36.0"
  }
}
