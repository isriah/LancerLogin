# Handoff

## Delivered

- Fresh public Apache-2.0 repository: `https://github.com/isriah/LancerLogin`; no source, credentials, configuration, resources, commits, or history from the reference installation were copied.
- Runnable Cloudflare Worker/D1 API, Vite/React dashboard, Raspberry Pi service, shared domain package, CI, release workflow, and public documentation site.
- Guarded adopter-owned Cloudflare provisioning verifies an account-owned token against the selected `CLOUDFLARE_ACCOUNT_ID`, refuses Create collisions, supports explicit Resume including interrupted secret setup, generates installation secrets, migrates D1, and deploys Worker/Pages. The first live Create run passed against the user-approved `lancerlogin-test` target; no account identifier or credential is stored in the repository.
- Local salted-scrypt and Google OAuth authentication; JSON/CORS-protected first-Admin bootstrap; immediate active-user/current-role session revalidation; Admin/Operator enforcement; user administration; Google-only lockout prevention; interactive local recovery.
- Shared onboarding, explicit themed/light/dark branding, roster, explicit one-kiosk replacement, retained heartbeat/reader/release health, R503 in-sensor enrollment and slot-only matching, an 800×480 touch UI, atomic offline queue, meetings, attendance, reasoned corrections/excuses, formula-safe CSV, typed-confirmation deletion, and D1 backup/restore.
- Setup feedback round implemented: the core flow is now a one-task-at-a-time, skippable/resumable wizard; Google OAuth has direct beginner links and exact origin/callback instructions; local password confirmation is required; dark mode retains access to branding; errors render next to the affected step in high contrast and receive focus; Discord IDs are optional and malformed values warn without blocking roster import.
- Dashboard feedback batch implemented: categories are independent deep-linkable pages; Roster is top-level and may link members to Admin/Operator accounts while preserving non-rostered staff accounts; Data settings provide separate backup, restore, and delete controls for meetings/attendance, roster, and the entire installation; onboarding ends with accessible confetti and a completion dialog; and intermediate-width cards shrink without escaping parent panels.
- A browser simulator pairs with a simulator-specific one-time code, has no production kiosk credential or active-kiosk count, toggles online/offline, and submits Admin-only check-ins only to visibly marked test meetings. It does not claim fingerprint, Pi, UART, Chromium, or physical queue acceptance.
- The GitHub workflow is now **Install or upgrade LancerLogin**. Its explicit Upgrade operation requires the existing named D1/Worker/Pages resources and retained secrets, applies migrations, preserves D1 data, and redeploys Worker/Pages.
- **Settings → Updates** shows installed/latest release status, downloads a whole-installation pre-update backup, and opens that workflow for explicit user authorization. It has no deployment credential and cannot dispatch an update.
- Pages collision discovery uses Cloudflare's authenticated account API instead of Wrangler's unstable human-facing list output. Worker secret discovery uses Wrangler's current `--format json` interface, retaining compatibility with newer Wrangler releases without reading or displaying secret values.
- User-facing privacy language is **anonymous usage reporting**. The first-Admin checkbox is enabled by default with a clear opt-out; the existing strictly allowlisted sender and isolated collector boundaries remain unchanged.
- Encrypted Google/Resend/Discord administration plus mocked/tested provider workflows. The single Discord kiosk-status message updates automatically from authenticated heartbeat state and a five-minute offline reconciliation without blocking kiosk operation. Consent-gated telemetry allowlists its payload and never reads raw IP into it. A separate collector-only deployment uses HMAC install identifiers, daily deduplication, edge limits, authenticated aggregates, k-anonymous metros, and scheduled retention; its live workflow passed health and synthetic report/aggregate/deletion checks.
- Accessible, task-first static documentation with browser-verified annotated dashboard, GitHub, Cloudflare, OAuth, kiosk, and integration visuals plus the community support contact.

## Verification boundary

All current automated tests, workspace typechecks, sanitizer checks, dependency audit, and production builds pass locally. CI and tagged releases run `npm run verify:migrations`, which applies and inspects the complete migration chain on a fresh isolated local D1 database without credentials or remote access. Browser verification covered the no-cloud setup preview, explicit themed branding plus a temporary dark override, retained kiosk health, public documentation, and the local kiosk at the Waveshare-sized 800×480 viewport. Provider calls use mocked responses in tests. The R503 protocol uses an injected mock transport. The community collector was deployed to its collector-only target and live-tested. Adopter provisioning run [#1](https://github.com/isriah/LancerLogin/actions/runs/33453004522) passed for the user-approved `lancerlogin-test` target: collision checks, fresh D1 creation/migrations, generated Worker secrets, Worker deployment, dashboard build, Pages creation, and Pages deployment all succeeded. Guarded upgrade run [#4](https://github.com/isriah/LancerLogin/actions/runs/33457799646) then applied the simulator migration and redeployed the existing isolated Worker and Pages project without rotating secrets or replacing D1. Live verification confirmed `https://lancerlogin-test-dashboard.pages.dev`, a ready same-origin health response, the paginated setup wizard, brand controls remaining reachable under a dark override, and the browser-simulator pairing choice. The present feedback batch has local/mock verification only: by explicit user request, no workflow was dispatched and the test deployment was not updated. No physical Pi/sensor run has occurred. `docs/COMPLETION-AUDIT.md` is the durable requirement-to-evidence matrix.

## Remaining acceptance

1. Manually run the guarded GitHub Upgrade workflow against the isolated `lancerlogin-test` resources and acceptance-test this feedback batch. Then run the Raspberry Pi/R503 hardware smoke checklist when a dedicated test Pi is available. Never bind, route, migrate, or restore data from the existing attendance resources or collector-only account.

## Live adopter test boundary

On 2026-08-31 the user explicitly revised the deployment boundary to allow a coexistence test in the same Cloudflare account as an earlier attendance deployment, while continuing to prohibit any modification, binding, route, database access, credential reuse, or dependency involving the earlier resources. A fresh seven-day account-owned token was created with only Workers Scripts Write, D1 Write, Pages Write, and Account Settings Read and stored as encrypted secrets in the GitHub `production` environment. Pre- and post-deployment inventories showed the legacy Worker, Pages project, and D1 database unchanged alongside the three newly named LancerLogin resources. No custom domain, DNS record, Worker route, shared binding, or migration path was configured.

The first two upgrade trials failed closed during read-only resource discovery because Wrangler changed Pages-list and secret-list command interfaces. Neither trial reached migrations or deployment. The workflow now uses Cloudflare's authenticated Pages API and Wrangler's supported secret-list format; the subsequent guarded upgrade succeeded only after confirming all three exact `lancerlogin-test-*` resources and both retained Worker secret names.
